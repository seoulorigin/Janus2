apiVersion: cilium.io/v1alpha1
kind: TracingPolicy
metadata:
  name: janus-syscall-hooking
  namespace: cl-security
spec:
  tracepoints:
    - subsystem: "raw_syscalls"
      event: "sys_enter"
      args:
        - index: 4
          type: "int64"